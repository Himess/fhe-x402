Do ALL of these in parallel. No phases, no waiting. Execute everything at once.

COMPLETE ALL 14 ITEMS:

V4.2 COMPLETION:
1. Finish confidentialTransferAndCall + onConfidentialTransferReceived callback
2. Update SDK fhePaymentHandler for single TX flow
3. Update fhePaywallMiddleware for single TX verification
4. Write tests for V4.2

DOCS UPDATE:
5. Update LIGHTPAPER.md for V4.0 (remove pool refs, add fee model, add ERC roadmap)
6. Update SECURITY.md for V4.0

ERC-8004 INTEGRATION (use existing Sepolia contracts):
7. Identity Registry (0xf66e7CBdAE1Cb710fee7732E4e1f173624e137A7)
   - SDK: registerAgent(name, agentURI, metadata)
   - agentURI: { x402Support: true, scheme: "fhe-confidential-v1", services: [...] }
   - SDK: setAgentWallet(agentId, walletAddress)
8. Reputation Registry (0x6E2a285294B5c74CB76d76AB77C1ef15c2A9E407)
   - SDK: giveFeedback(agentId, score, tags, proofOfPayment)
   - Auto-feedback after successful x402 payment in middleware
   - SDK: getAgentReputation(agentId) for trust check
9. Tests for ERC-8004

ERC-8183 AGENTIC COMMERCE (write from spec, no ref impl exists):
10. ACP contract:
    - createJob, setBudget, fund, submit, complete, reject, claimRefund
    - 3 roles: Client, Provider, Evaluator
    - Lifecycle: Open → Funded → Submitted → Completed/Rejected
    - Platform fee: 1% on completion (escrow-locked, unbypassable)
    - All events: JobCreated, BudgetSet, JobFunded, JobSubmitted, JobCompleted, JobRejected
11. IACPHook for cUSDC:
    - afterAction(fund): cUSDC confidential escrow
    - afterAction(complete): release to provider minus 1% fee to treasury
12. SDK helpers: createConfidentialJob, fundJob, submitDeliverable, completeJob, rejectJob
13. Tests for ERC-8183 (30+ tests)
14. Deploy ALL new contracts to Sepolia

AFTER ALL 14 ARE DONE:
- Run full test suite. Report total count.
- Verify every contract compiles
- Verify every test passes
- Check SDK exports are correct
- Check middleware works with new flow
- If ANYTHING fails, fix it immediately

ROADMAP ONLY (add to README, do NOT implement):
- ERC-8126 Risk Scoring
- Encrypted reputation (FHE + ERC-8004)
- Zaiffer cUSDC migration
- Cross-chain L2
- Multi-token factory

Target: 280+ total tests, all passing. Start now, don't stop until all 14 items complete and verified.