Audit report reviewed. Do Tier 1 + Tier 2 now. Fix everything, then verify.

TIER 1 — CRITICAL + HIGH FIXES:
1. C-1: recordPayment — remove payer param, use msg.sender
2. C-2: recordBatchPayment — same fix, use msg.sender  
3. C-3: confidentialTransferAndCall — use parent IERC7984Receiver interface, remove IConfidentialTransferCallback
4. H-1: wrap() — add SafeCast.toUint64(amount), revert on overflow
5. H-2: ACP hook callbacks — wrap in try/catch with 100_000 gas limit
6. H-3: ACP setBudget — restrict to client only (if msg.sender != job.client revert)
7. H-4: onConfidentialTransferReceived — add trustedToken check (msg.sender == trustedToken)
8. M-1: finalizeUnwrap — add whenNotPaused or document why intentional
9. M-2: confidentialTransferAndCall — add nonReentrant
10. M-3: ACP — add Pausable
11. M-4: Fee rate() consistency between wrap and unwrap
12. M-5: claimRefund — add job.client access control or document
13. M-6: Remove beforeAction from IACPHook (dead interface)
14. Write missing tests:
    - wrap(amount > uint64.max) reverts
    - recordPayment with spoofed payer (should use msg.sender)
    - onConfidentialTransferReceived from untrusted token (should revert)
    - Hook DoS (malicious hook reverts, complete still works via try/catch)
    - setBudget by non-client (should revert)
    - Nonce griefing scenario
    - ACP pause/unpause

TIER 2 — CODE CLEANUP:
15. Remove IConfidentialTransferCallback interface file
16. Remove unused Ownable import (ConfidentialUSDC:11)
17. Export FEE_BPS and MIN_PROTOCOL_FEE constants from SDK
18. Fix ACP_ABI tuple — add 9th field (hook)
19. Replace rate limiter with LRU cache (fix memory leak)
20. SDK fheFetch — change linear backoff to exponential backoff
21. Add batch total overflow check: perRequest * requestCount <= uint64.max
22. Facilitator /verify — add network validation check

After ALL fixes:
- Run full test suite
- Report new total test count (target 340+)
- Verify zero compilation warnings
- Confirm all CRITICAL and HIGH issues resolved

ADD TO ROADMAP in README (Tier 3 + 4):
- UUPS proxy pattern for upgradeability
- Multisig treasury (Gnosis Safe)
- KMS emergency withdrawal timelock
- The Graph event indexer
- Gas benchmark report
- Formal verification (Certora/Halmos)
- L2 deployment
- x402 Foundation membership
- ERC-8183 reference implementation ownership
- Zama partnership
- LangChain/CrewAI/AutoGPT integrations

Start now. Fix everything in one batch.